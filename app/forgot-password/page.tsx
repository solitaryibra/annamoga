import AuthForm from "../../components/AuthForm";

export const metadata = { title: "Reset Password | Anna Moga" };

export default function ForgotPasswordPage() {
  return <AuthForm mode="forgot" />;
}
