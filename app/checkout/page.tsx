import CheckoutClient from "../../components/CheckoutClient";

export const metadata = {
  title: "Checkout | Anna Moga",
  description: "Anna Moga checkout.",
};

export default function CheckoutPage() {
  return <CheckoutClient />;
}
