import CartClient from "../../components/CartClient";

export const metadata = {
  title: "Cart | Anna Moga",
  description: "Review your Anna Moga cart.",
};

export default function CartPage() {
  return <CartClient />;
}
